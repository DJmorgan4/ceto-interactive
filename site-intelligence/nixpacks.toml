[phases.setup]
nixPkgs = ["gdal", "python311", "pango", "cairo", "gdk-pixbuf", "libffi", "gobject-introspection", "expat", "libxml2"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
